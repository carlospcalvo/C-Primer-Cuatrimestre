#include <stdio.h>
#include <stdlib.h>

int main()
{
    int vec[tam] = {7,5,8,1,3};
    int aux;

    for (int x = 0; x < tam-1; x++ ){

        for(int y = )

    }

    return 0;
}
